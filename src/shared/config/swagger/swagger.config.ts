import { SwaggerConfig } from './swagger.inerface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'InteviewProject',
  description: 'Template',
  version: '1.0.0',
  tags: ['Template'],
};
